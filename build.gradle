import au.id.tmm.tmmUtils.build.MyVersionPlugin

apply plugin: 'io.codearte.nexus-staging'
apply plugin: MyVersionPlugin
apply plugin: 'idea'

allprojects {
    group = 'au.id.tmm'
}

wrapper {
    distributionType = Wrapper.DistributionType.ALL
    gradleVersion = '4.10.2'
}
